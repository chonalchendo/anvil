# Python-generated files
__pycache__/
*.py[oc]
/build/
/dist/
/wheels/
*.egg-info

# Virtual environments
.venv

# Skill-creator eval workspaces (regenerable from skills/<name>/evals/evals.json)
skills/*-workspace/

# Superpowers working artifacts (specs, plans, etc. — not committed)
docs/superpowers/

# Personal scratchpads (kept on disk, not in source tree)
docs/IDEAS.md
docs/first_principles_anvil.md
docs/implementation_plan.md

# Git worktrees
.worktrees/

# Claude Code local state (worktrees, scheduled-task lock, etc.)
# Keep settings.json and hooks/ tracked so they ship with the repo.
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**
