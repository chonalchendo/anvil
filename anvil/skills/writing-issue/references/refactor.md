# Refactor issue — body guidance

No `reproduction_anchor` — nothing is broken.

Lead `## Problem` with the invariant that must hold across the change (the external behaviour or contract), the shape change you intend, and the regression risk it carries. A refactor that changes behaviour is a different issue.
